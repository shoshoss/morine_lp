# CODEX_TASK_01.md

## 目的

モリーヌLPの初回実装として、次の範囲だけを完成させてください。

1. ページの基本骨格
2. ヘッダー
3. ファーストビュー
4. 安心情報
5. スマートフォン固定CTA

このタスクでは、悩み、特徴、メニュー、口コミ、FAQ、アクセス、フッター本文は実装しません。

---

## 最初に確認するファイル

必要箇所だけ読んでください。

- `AGENTS.md`
- `docs/LP_BRIEF.md`
  - 1～5章
  - 6章の「ファーストビュー」「安心情報」「スマートフォン固定CTA」
  - 7～9章
- `docs/CONTENT_MASTER.md`
  - `A-01`
  - `A-04`
  - `A-06`～`A-10`
  - `A-14`
  - `C-01`～`C-05`
  - `J`
  - `K-03`
- `docs/IMAGE_ASSET_MAP.md`
  - ファーストビュー候補
- 現在の`index.html`、CSS、JS

資料全体の要約は不要です。

---

## 実装内容

### 1. 基本骨格

既存コードがある場合はその構成を維持してください。

存在しない場合のみ、次の最小構成を使用してください。

```text
index.html
css/style.css
js/main.js
images/
docs/
```

不要なフレームワーク、ビルド環境、外部ライブラリは追加しないでください。

### 2. ヘッダー

- モリーヌのロゴまたは店舗名
- ファーストビューを圧迫しない高さ
- モバイルで崩れない
- 主CTAを置く場合は小さく1つだけ
- ハンバーガーメニューは、リンク先セクションが未実装のため作らない

使用候補：

`images/logo-morine.webp`

低解像度のため大きく表示しないでください。

### 3. ファーストビュー

`CONTENT_MASTER.md`の次のIDを使用してください。

- `hero.eyebrow`
- `hero.heading`
- `hero.lead`
- `hero.trust_items`
- `cta.first_view`

各主要要素に対応する`data-content-id`を付けてください。

必須情報：

- しっかり圧
- 力加減を相談できる
- 本通駅から徒歩約3分
- 10時～22時
- 男性利用可能
- ホットペッパーCTA

画像方針：

- コピーとCTAを主役にする
- 低解像度画像を全幅背景にしない
- `images/therapist-body-treatment.webp`を限定幅で添える、または`images/salon-room.webp`を最大表示幅以内で使用する
- 画像なしでも成立するレイアウトにする

CTA：

- 文言：`ホットペッパーで空き状況を見る`
- URL：`CONTENT_MASTER.md`の`booking.hotpepper_url`
- `data-cta-position="first-view"`
- 外部ページへ移動する補足を近くに表示
- 新しいタブで開く場合は安全な`rel`を設定

### 4. 安心情報

ファーストビュー直後に、読みやすい短い表示で次を配置してください。

- 1対1の貸切空間
- 施術着あり
- 当日・直前予約歓迎
- 無理な勧誘なし

年中無休と男性利用可能は、ファーストビューと重複しすぎないよう整理してください。

アイコンライブラリは追加しないでください。CSS、文字、簡単なSVGのいずれかで十分です。

### 5. スマートフォン固定CTA

- 文言：`空き状況を見る`
- 補足：`ホットペッパー`
- `data-cta-position="sticky"`
- モバイルのみ表示
- ファーストビュー内のCTAが見えている間は非表示、または邪魔にならない設計
- 本文を隠さないように下余白を確保
- JavaScriptを使う場合は依存なしの最小実装
- JavaScriptが失敗しても通常CTAは利用可能にする

---

## デザイン

- モバイルファースト
- 深い青緑を主色
- 白または薄い生成りの背景
- ゴールドは小さなアクセントのみ
- 清潔、誠実、親しみやすい
- 女性向けに偏りすぎない
- 力強い施術だが、怖く見せない
- 装飾よりコピーとCTAを優先
- 過剰な影、グラデーション、アニメーションは不要

---

## 確認

最低限、次を確認してください。

- 390pxで横スクロールがない
- 360pxで見出しとCTAが切れない
- PC幅で低解像度画像が不自然に拡大されない
- 予約URLが正しい
- 画像パスが正しい
- 固定CTAが本文を隠さない
- `[要確認]`の情報が表示されていない
- 次の禁止表現がない
  - 治す
  - 根本改善
  - 必ず改善
  - No.1
  - 予約殺到

---

## 完了報告

ファイル全文を貼らず、次の形式だけで回答してください。

```text
完了:
- 変更内容を3項目以内

確認:
- 確認結果を3項目以内

保留:
- ブロッカーがある場合のみ
```

重大なブロッカーがない限り、質問せず実装を完了してください。
