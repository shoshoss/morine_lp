# AGENTS.md

## 1. Project

リラクゼーションサロン「モリーヌ」の新規予約獲得用LPを制作する。

初回公開の最優先目的は、初めての閲覧者が不安なく内容を理解し、ホットペッパービューティーの予約ページへ進むこと。

次を同時に重視する。

1. 予約につながること
2. 来店後の期待違いを減らすこと
3. 誇張や未確認情報を公開しないこと
4. 小さく完成させ、後から改善しやすいこと
5. 制作時間と出力トークンを浪費しないこと

---

## 2. Source of truth

情報の優先順位は次のとおり。

1. `docs/CONTENT_MASTER.md`
2. `docs/LP_BRIEF.md`
3. `docs/IMAGE_ASSET_MAP.md`
4. 本ファイル
5. 既存コード

競合する情報がある場合、上位を優先する。

### 公開可能な内容

`CONTENT_MASTER.md`で次の状態だけを公開画面に使用する。

- `[公開可]`
- `[公開可・森さん確認済み]`
- `[口コミ根拠]`
- `[原稿案]`のうち、`LP_BRIEF.md`で初回実装に指定されたもの

次は公開しない。

- `[要確認]`
- `[非公開]`
- `[管理用]`
- 確認待ちの価格、徒歩時間、効果、予約条件

推測で不足情報を補わない。

---

## 3. Initial release strategy

初回公開版は次で固定する。

- 主CTA：ホットペッパービューティー
- CTA文言：`ホットペッパーで空き状況を見る`
- 直接予約、LINE、電話、Instagramは主CTAにしない
- 年代や性別ではなく、悩み別に案内する
- ファーストビューはコピー主体
- 低解像度写真を全幅へ拡大しない
- 本通駅の表記は暫定で`徒歩約3分`
- 予約導線の微調整は初回完成後に行う

主CTA URLは`CONTENT_MASTER.md`の`booking.hotpepper_url`を使用する。

---

## 4. Working method

### 4.1 1回の依頼につき1タスク

依頼された範囲だけを実装する。

- 次のセクションを先回りして作らない
- 頼まれていない全面リファクタリングをしない
- 頼まれていないライブラリやビルド環境を追加しない
- 頼まれていない文書を新規作成しない

依頼外で重大な問題を見つけた場合は、修正せず最後に1行で報告する。

### 4.2 最小限のファイルだけ読む

作業開始時に必ず読む。

- `AGENTS.md`
- 今回の指示文
- 今回必要な範囲の`docs/LP_BRIEF.md`
- 今回必要な項目の`docs/CONTENT_MASTER.md`
- 使用画像がある場合のみ`docs/IMAGE_ASSET_MAP.md`
- 変更対象のコード

毎回すべての資料を全文で読み直す必要はない。見出し、ID、キーワード検索を使い、今回の範囲だけ確認する。

### 4.3 最小差分

- 既存の正常な箇所を変更しない
- 同じ内容を複数ファイルへ重複保存しない
- 原稿の正解はMarkdown、表示はHTMLに置く
- CSSは既存ルールを再利用する
- JSが不要なら追加しない
- 実装途中の仮文言を残さない

### 4.4 迷った場合

次の順で判断する。

1. `CONTENT_MASTER.md`
2. `LP_BRIEF.md`
3. 予約者の迷いを減らす方
4. 実装が単純な方
5. 後から変更しやすい方

公開情報に影響する不明点だけ質問する。装飾上の軽微な判断は、既存デザインと本ファイルに従って進める。

---

## 5. Output economy

Codexの最終報告は短くする。

### 報告形式

```text
完了:
- 変更内容を3項目以内

確認:
- 実行した確認を3項目以内

保留:
- ブロッカーがある場合のみ
```

### 出力しないもの

- 変更していないコード
- ファイル全文
- 資料の長い要約
- 思考過程
- 一般論
- 同じ説明の繰り返し
- 大量のスクリーンショット
- 頼まれていない改善案の一覧

コード変更はファイルへ直接反映し、回答には差分の要点だけを書く。

---

## 6. Conversion rules

### 6.1 ファーストビュー

最初の画面で次を伝える。

- しっかりした圧
- 力加減を相談できる
- 本通駅から徒歩約3分
- 10時～22時
- 男性利用可能
- 予約CTA

「強い施術」だけを強調せず、調整できる安心を近くに置く。

### 6.2 CTA

- 1画面の主目的を1つにする
- 主ボタンは同じ色、同じ文言、同じURLを使う
- `data-cta-position`を付ける
- 外部予約ページへ移動することが分かるようにする
- 偽の緊急性や残数表示を使わない

### 6.3 口コミ

- 一人の口コミを別の人と合成しない
- 原文の意味を変えない
- 個人の感想であることを分かる形にする
- サロン側の効果保証へ書き換えない

### 6.4 表現

使用しない。

- 治す
- 治療
- 根本改善
- 必ず改善
- 絶対に揉み返しがない
- No.1
- 地域一番
- 予約殺到
- 根拠のない最上級
- 不安をあおる表現

---

## 7. Image rules

画像は`images/`内のWebPを使用する。

参照元、alt、最大表示幅は`docs/IMAGE_ASSET_MAP.md`を確認する。

### 必須

- 日本語の元ファイル名をHTMLから参照しない
- `width`と`height`を指定する
- ファーストビュー以外は原則`loading="lazy"`
- 低解像度画像を指定幅以上に拡大しない
- 顔や施術の手元を不自然に切らない
- 画像内容と一致するaltを付ける

### 禁止

- ホットペッパー掲載写真
- 旧営業時間・旧クーポン告知画像
- LINE友だち追加画像を主導線に使うこと
- 重複画像の同時使用
- 実店舗と誤認させる素材写真

---

## 8. Technical policy

既存技術を優先する。静的HTML、CSS、JavaScriptで足りる場合はフレームワークを導入しない。

### HTML

- セマンティックな要素を使用する
- 見出し階層を飛ばさない
- 主要原稿に`data-content-id`を付ける
- CTAに`data-cta-position`を付ける
- ボタン用途とリンク用途を分ける
- 外部リンクは安全な属性を設定する

### CSS

- モバイルファースト
- 360px、390px、430px、PCで破綻しない
- 横スクロールを出さない
- タップ領域を小さくしない
- 色は深い青緑、白・生成り、淡いグレー、控えめなゴールドを基準にする
- 過剰な影、グラデーション、アニメーションを避ける
- 同じ数値を繰り返す場合はCSSカスタムプロパティを使う

### JavaScript

JSなしで実現できる場合は使わない。

使う場合：

- 依存ライブラリを増やさない
- エラー時も本文とCTAを利用できる
- イベントを重複登録しない
- 固定CTAやアコーディオンなど必要最小限にする

### Performance

- 不要なフォント、動画、外部スクリプトを追加しない
- 画像はWebPを使用する
- ファーストビュー以外を遅延読み込みする
- レイアウトシフトを防ぐ
- 1つの装飾のために大きなライブラリを入れない

---

## 9. Validation

各タスク後、変更範囲に必要な確認だけ行う。

### 常に確認

- HTML、CSS、JSの構文上の明確な問題
- 変更したリンクと画像パス
- 未確認情報が表示されていないこと
- 横スクロールがないこと
- CTAが操作できること

### 画面確認

変更箇所に応じて最小限で行う。

- モバイル：390pxを必須
- 追加確認：360pxまたは430px
- PC：1440px相当

毎回すべての幅で大量の画像を保存しない。レイアウト変更が広い場合だけ追加確認する。

---

## 10. Do not overbuild

初回版では実装しない。

- CMS
- 会員登録
- 独自予約システム
- ブログ
- 診断コンテンツ
- スライダー
- 自動再生動画
- 複雑なアニメーション
- 毎日更新が必要な空き枠表示
- ホットペッパーと直接予約の同列表示
- 全メニュー、全口コミの掲載
- 計測基盤の過剰実装

---

## 11. Definition of done

タスク完了とは、次を満たす状態。

- 指示された範囲だけが実装されている
- `CONTENT_MASTER.md`と矛盾しない
- CTA、画像、文言が仮のままではない
- モバイルで読みやすく操作できる
- 未確認・禁止表現が画面にない
- 既存の正常箇所を壊していない
- 次のタスクが小さな差分で進められる
